// for

for (let i = 1; i <= 5; i++) {
    const element = i;
    console.log(element);   
}

// while

let index = 0
while (index<=5) {
    console.log(`Value: ${index}`);
    index++;
}

do {
    console.log(`Value: ${index}`);
    index++;
} while (index<=10);